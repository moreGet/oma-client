# 변경 이력 (Changelog)

이 프로젝트의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전 체계는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

버전 의미(이 프로젝트 기준):
- **MAJOR** — 서버 API 계약 비호환, 설정 스키마 비호환 마이그레이션
- **MINOR** — 호환 유지 기능 추가
- **PATCH** — 버그 수정·UI 다듬기

> 참고: 설정 데이터 호환성은 앱 버전과 별개로 `AppSettings.SchemaVersion`(현재 5)으로 추적합니다.

---

## [1.3.0] - 2026-06-27

이 버전부터 명시적 버전 관리(`.csproj <Version>`)를 도입했습니다. 이전 이력은 git 커밋으로만 추적되었습니다.

### 추가 (Added)
- **멀티루트 작업 디렉토리**: 최대 10개 폴더를 작업 공간으로 등록, 폴더별 접근 허용/차단 토글. 에이전트 샌드박스가 활성 루트 전체를 OR 검증.
- **프로젝트(대화 묶음)**: 대화 세션을 프로젝트로 그룹화. 로컬 우선 저장 + 선택적 서버 동기화. 사이드바에서 **드래그앤드롭으로 대화 분류**.
- **서버 프로필 조회**: 사용자 이름·조직·이메일을 서버(`GET /api/v1/users/me`)에서 받아 설정창에 표시(읽기 전용).
- **버전 관리**: SemVer 도입, 빌드 시 git 해시를 InformationalVersion에 부착. 설정창에 버전 표시. 서버 버전 점검(`GET /api/v1/client/version`)으로 업데이트 알림.
- **디자인 토큰**: 타이포·모서리·간격 토큰화(`Resources/Tokens.xaml`) + 디자이너 가이드(`docs/design-tokens.md`).
- 신규 도구 `list_processes_memory_kb`(프로세스 메모리 KB 표시).

### 변경 (Changed)
- 로그인을 **시작 게이트(LoginWindow)로 일원화**. 설정창의 사용자 ID/비밀번호 입력 제거(로그아웃·상태만 유지).
- 폴더 접근 UI를 체크박스 → **토글 스위치**로.
- 채팅 트랜스크립트·입력창이 **창 크기에 맞춰 늘어나도록**(반응형). 고정 폭(760) 제거.
- 작업 디렉토리 칩이 활성 디렉토리 2개 이상일 때 **"외 N개"** 표시.

### 제거 (Removed)
- **최대 토큰(MaxTokens) 클라 설정 제거** — 서버가 제어(와이어 `max_tokens`는 기본값 전송).

### 수정 (Fixed)
- 대화 삭제가 사이드바에 즉시 반영되지 않던 문제.
- 프로젝트에 분류한 대화를 이어 채팅·저장하면 미분류로 되돌아가던 문제(저장 시 `ProjectId`/`CreatedUtc` 보존).
- 설정에서 작업 디렉토리를 추가해도 메인 칩이 갱신되지 않던 문제(설정 변경 구독).
- "로그인 필요" 배너가 (로그인이 사라진) 설정창을 열던 회귀 → 로그인 게이트 재오픈으로 복구.

### 보안 (Security)
- 멀티루트에서도 심볼릭링크/정션 탈출 방어(RealPath 2차 검증) 유지. 비활성(토글 off) 루트는 샌드박스 통과 불가.

---

## 버전 올리는 법
1. `.csproj`의 `<Version>` 수정 (예: `1.3.0` → `1.4.0`)
2. 이 파일(CHANGELOG)에 변경점 추가
3. 커밋 후 태그: `git tag v1.4.0 && git push origin v1.4.0`
4. GitHub Release 작성(이 변경점 복사)
